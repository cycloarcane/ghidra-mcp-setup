{
  "_comment": "Replace __INSTALL_DIR__ with the absolute path of this repo. Drop the mcpServers block into ~/.gemini/settings.json (verify path against current gemini-cli docs).",
  "mcpServers": {
    "ghidra": {
      "command": "__INSTALL_DIR__/venv/bin/python",
      "args": [
        "__INSTALL_DIR__/GhidraMCP/bridge_mcp_ghidra.py",
        "--ghidra-server",
        "http://127.0.0.1:8080/"
      ]
    }
  }
}
