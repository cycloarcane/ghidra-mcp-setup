# Python venv created by install.sh
venv/

# Cloned bridge source (re-cloned by install.sh)
GhidraMCP/

# Downloaded Ghidra plugin releases (re-fetched by install.sh)
extensions/*.zip
extensions/*.tar.gz

# Configs with absolute paths baked in (re-generated by install.sh)
configs/generated/

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
*.swp
__pycache__/

# Ghidra project state (don't commit user analysis projects into this repo)
*.gpr
*.rep/
*.lock
*.lock~

# Test binaries / sample inputs users drop in to try the setup
ghidratestprioject/
samples/
